// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.domain;

import ch.emineo.roo.reverse.domain.Advancenotice;
import ch.emineo.roo.reverse.domain.AdvancenoticeDataOnDemand;
import ch.emineo.roo.reverse.domain.AdvancenoticeIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AdvancenoticeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AdvancenoticeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AdvancenoticeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AdvancenoticeIntegrationTest: @Transactional;
    
    @Autowired
    private AdvancenoticeDataOnDemand AdvancenoticeIntegrationTest.dod;
    
    @Test
    public void AdvancenoticeIntegrationTest.testCountAdvancenotices() {
        Assert.assertNotNull("Data on demand for 'Advancenotice' failed to initialize correctly", dod.getRandomAdvancenotice());
        long count = Advancenotice.countAdvancenotices();
        Assert.assertTrue("Counter for 'Advancenotice' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AdvancenoticeIntegrationTest.testFindAdvancenotice() {
        Advancenotice obj = dod.getRandomAdvancenotice();
        Assert.assertNotNull("Data on demand for 'Advancenotice' failed to initialize correctly", obj);
        Integer id = obj.getAdvancenoticeid();
        Assert.assertNotNull("Data on demand for 'Advancenotice' failed to provide an identifier", id);
        obj = Advancenotice.findAdvancenotice(id);
        Assert.assertNotNull("Find method for 'Advancenotice' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Advancenotice' returned the incorrect identifier", id, obj.getAdvancenoticeid());
    }
    
    @Test
    public void AdvancenoticeIntegrationTest.testFindAllAdvancenotices() {
        Assert.assertNotNull("Data on demand for 'Advancenotice' failed to initialize correctly", dod.getRandomAdvancenotice());
        long count = Advancenotice.countAdvancenotices();
        Assert.assertTrue("Too expensive to perform a find all test for 'Advancenotice', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Advancenotice> result = Advancenotice.findAllAdvancenotices();
        Assert.assertNotNull("Find all method for 'Advancenotice' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Advancenotice' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AdvancenoticeIntegrationTest.testFindAdvancenoticeEntries() {
        Assert.assertNotNull("Data on demand for 'Advancenotice' failed to initialize correctly", dod.getRandomAdvancenotice());
        long count = Advancenotice.countAdvancenotices();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Advancenotice> result = Advancenotice.findAdvancenoticeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Advancenotice' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Advancenotice' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AdvancenoticeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Advancenotice' failed to initialize correctly", dod.getRandomAdvancenotice());
        Advancenotice obj = dod.getNewTransientAdvancenotice(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Advancenotice' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Advancenotice' identifier to be null", obj.getAdvancenoticeid());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Advancenotice' identifier to no longer be null", obj.getAdvancenoticeid());
    }
    
    @Test
    public void AdvancenoticeIntegrationTest.testRemove() {
        Advancenotice obj = dod.getRandomAdvancenotice();
        Assert.assertNotNull("Data on demand for 'Advancenotice' failed to initialize correctly", obj);
        Integer id = obj.getAdvancenoticeid();
        Assert.assertNotNull("Data on demand for 'Advancenotice' failed to provide an identifier", id);
        obj = Advancenotice.findAdvancenotice(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Advancenotice' with identifier '" + id + "'", Advancenotice.findAdvancenotice(id));
    }
    
}
