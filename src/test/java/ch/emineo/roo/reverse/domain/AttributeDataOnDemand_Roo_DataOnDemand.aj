// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.domain;

import ch.emineo.roo.reverse.domain.Attribute;
import ch.emineo.roo.reverse.domain.AttributeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect AttributeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AttributeDataOnDemand: @Component;
    
    private Random AttributeDataOnDemand.rnd = new SecureRandom();
    
    private List<Attribute> AttributeDataOnDemand.data;
    
    public Attribute AttributeDataOnDemand.getNewTransientAttribute(int index) {
        Attribute obj = new Attribute();
        setLanguage(obj, index);
        setType(obj, index);
        setValue(obj, index);
        return obj;
    }
    
    public void AttributeDataOnDemand.setLanguage(Attribute obj, int index) {
        String language = "language_" + index;
        if (language.length() > 255) {
            language = language.substring(0, 255);
        }
        obj.setLanguage(language);
    }
    
    public void AttributeDataOnDemand.setType(Attribute obj, int index) {
        String type = "type_" + index;
        if (type.length() > 255) {
            type = type.substring(0, 255);
        }
        obj.setType(type);
    }
    
    public void AttributeDataOnDemand.setValue(Attribute obj, int index) {
        String value = "value_" + index;
        obj.setValue(value);
    }
    
    public Attribute AttributeDataOnDemand.getSpecificAttribute(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Attribute obj = data.get(index);
        Long id = obj.getAttributeid();
        return Attribute.findAttribute(id);
    }
    
    public Attribute AttributeDataOnDemand.getRandomAttribute() {
        init();
        Attribute obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getAttributeid();
        return Attribute.findAttribute(id);
    }
    
    public boolean AttributeDataOnDemand.modifyAttribute(Attribute obj) {
        return false;
    }
    
    public void AttributeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Attribute.findAttributeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Attribute' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Attribute>();
        for (int i = 0; i < 10; i++) {
            Attribute obj = getNewTransientAttribute(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
