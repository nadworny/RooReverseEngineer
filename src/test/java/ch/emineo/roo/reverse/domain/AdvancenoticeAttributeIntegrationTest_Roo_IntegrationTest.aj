// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.domain;

import ch.emineo.roo.reverse.domain.AdvancenoticeAttribute;
import ch.emineo.roo.reverse.domain.AdvancenoticeAttributeDataOnDemand;
import ch.emineo.roo.reverse.domain.AdvancenoticeAttributeIntegrationTest;
import ch.emineo.roo.reverse.domain.AdvancenoticeAttributePK;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AdvancenoticeAttributeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AdvancenoticeAttributeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AdvancenoticeAttributeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AdvancenoticeAttributeIntegrationTest: @Transactional;
    
    @Autowired
    private AdvancenoticeAttributeDataOnDemand AdvancenoticeAttributeIntegrationTest.dod;
    
    @Test
    public void AdvancenoticeAttributeIntegrationTest.testCountAdvancenoticeAttributes() {
        Assert.assertNotNull("Data on demand for 'AdvancenoticeAttribute' failed to initialize correctly", dod.getRandomAdvancenoticeAttribute());
        long count = AdvancenoticeAttribute.countAdvancenoticeAttributes();
        Assert.assertTrue("Counter for 'AdvancenoticeAttribute' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AdvancenoticeAttributeIntegrationTest.testFindAdvancenoticeAttribute() {
        AdvancenoticeAttribute obj = dod.getRandomAdvancenoticeAttribute();
        Assert.assertNotNull("Data on demand for 'AdvancenoticeAttribute' failed to initialize correctly", obj);
        AdvancenoticeAttributePK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AdvancenoticeAttribute' failed to provide an identifier", id);
        obj = AdvancenoticeAttribute.findAdvancenoticeAttribute(id);
        Assert.assertNotNull("Find method for 'AdvancenoticeAttribute' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AdvancenoticeAttribute' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AdvancenoticeAttributeIntegrationTest.testFindAllAdvancenoticeAttributes() {
        Assert.assertNotNull("Data on demand for 'AdvancenoticeAttribute' failed to initialize correctly", dod.getRandomAdvancenoticeAttribute());
        long count = AdvancenoticeAttribute.countAdvancenoticeAttributes();
        Assert.assertTrue("Too expensive to perform a find all test for 'AdvancenoticeAttribute', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AdvancenoticeAttribute> result = AdvancenoticeAttribute.findAllAdvancenoticeAttributes();
        Assert.assertNotNull("Find all method for 'AdvancenoticeAttribute' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AdvancenoticeAttribute' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AdvancenoticeAttributeIntegrationTest.testFindAdvancenoticeAttributeEntries() {
        Assert.assertNotNull("Data on demand for 'AdvancenoticeAttribute' failed to initialize correctly", dod.getRandomAdvancenoticeAttribute());
        long count = AdvancenoticeAttribute.countAdvancenoticeAttributes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AdvancenoticeAttribute> result = AdvancenoticeAttribute.findAdvancenoticeAttributeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AdvancenoticeAttribute' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AdvancenoticeAttribute' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AdvancenoticeAttributeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'AdvancenoticeAttribute' failed to initialize correctly", dod.getRandomAdvancenoticeAttribute());
        AdvancenoticeAttribute obj = dod.getNewTransientAdvancenoticeAttribute(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'AdvancenoticeAttribute' failed to provide a new transient entity", obj);
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'AdvancenoticeAttribute' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AdvancenoticeAttributeIntegrationTest.testRemove() {
        AdvancenoticeAttribute obj = dod.getRandomAdvancenoticeAttribute();
        Assert.assertNotNull("Data on demand for 'AdvancenoticeAttribute' failed to initialize correctly", obj);
        AdvancenoticeAttributePK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AdvancenoticeAttribute' failed to provide an identifier", id);
        obj = AdvancenoticeAttribute.findAdvancenoticeAttribute(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AdvancenoticeAttribute' with identifier '" + id + "'", AdvancenoticeAttribute.findAdvancenoticeAttribute(id));
    }
    
}
