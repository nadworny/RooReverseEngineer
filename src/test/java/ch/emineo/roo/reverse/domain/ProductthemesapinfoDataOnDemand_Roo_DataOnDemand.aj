// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.domain;

import ch.emineo.roo.reverse.domain.Productthemesapinfo;
import ch.emineo.roo.reverse.domain.ProductthemesapinfoDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ProductthemesapinfoDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ProductthemesapinfoDataOnDemand: @Component;
    
    private Random ProductthemesapinfoDataOnDemand.rnd = new SecureRandom();
    
    private List<Productthemesapinfo> ProductthemesapinfoDataOnDemand.data;
    
    public Productthemesapinfo ProductthemesapinfoDataOnDemand.getNewTransientProductthemesapinfo(int index) {
        Productthemesapinfo obj = new Productthemesapinfo();
        setName(obj, index);
        setOnline(obj, index);
        setPublic1(obj, index);
        setSapnumber(obj, index);
        setWeight(obj, index);
        return obj;
    }
    
    public void ProductthemesapinfoDataOnDemand.setName(Productthemesapinfo obj, int index) {
        String name = "name_" + index;
        if (name.length() > 255) {
            name = name.substring(0, 255);
        }
        obj.setName(name);
    }
    
    public void ProductthemesapinfoDataOnDemand.setOnline(Productthemesapinfo obj, int index) {
        Boolean online = true;
        obj.setOnline(online);
    }
    
    public void ProductthemesapinfoDataOnDemand.setPublic1(Productthemesapinfo obj, int index) {
        Boolean public1 = true;
        obj.setPublic1(public1);
    }
    
    public void ProductthemesapinfoDataOnDemand.setSapnumber(Productthemesapinfo obj, int index) {
        Long sapnumber = new Integer(index).longValue();
        obj.setSapnumber(sapnumber);
    }
    
    public void ProductthemesapinfoDataOnDemand.setWeight(Productthemesapinfo obj, int index) {
        Integer weight = new Integer(index);
        obj.setWeight(weight);
    }
    
    public Productthemesapinfo ProductthemesapinfoDataOnDemand.getSpecificProductthemesapinfo(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Productthemesapinfo obj = data.get(index);
        Integer id = obj.getProductthemesapinfoid();
        return Productthemesapinfo.findProductthemesapinfo(id);
    }
    
    public Productthemesapinfo ProductthemesapinfoDataOnDemand.getRandomProductthemesapinfo() {
        init();
        Productthemesapinfo obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getProductthemesapinfoid();
        return Productthemesapinfo.findProductthemesapinfo(id);
    }
    
    public boolean ProductthemesapinfoDataOnDemand.modifyProductthemesapinfo(Productthemesapinfo obj) {
        return false;
    }
    
    public void ProductthemesapinfoDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Productthemesapinfo.findProductthemesapinfoEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Productthemesapinfo' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Productthemesapinfo>();
        for (int i = 0; i < 10; i++) {
            Productthemesapinfo obj = getNewTransientProductthemesapinfo(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
