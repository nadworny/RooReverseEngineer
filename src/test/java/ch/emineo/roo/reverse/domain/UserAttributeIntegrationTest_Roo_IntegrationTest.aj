// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.domain;

import ch.emineo.roo.reverse.domain.UserAttribute;
import ch.emineo.roo.reverse.domain.UserAttributeDataOnDemand;
import ch.emineo.roo.reverse.domain.UserAttributeIntegrationTest;
import ch.emineo.roo.reverse.domain.UserAttributePK;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserAttributeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserAttributeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserAttributeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserAttributeIntegrationTest: @Transactional;
    
    @Autowired
    private UserAttributeDataOnDemand UserAttributeIntegrationTest.dod;
    
    @Test
    public void UserAttributeIntegrationTest.testCountUserAttributes() {
        Assert.assertNotNull("Data on demand for 'UserAttribute' failed to initialize correctly", dod.getRandomUserAttribute());
        long count = UserAttribute.countUserAttributes();
        Assert.assertTrue("Counter for 'UserAttribute' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserAttributeIntegrationTest.testFindUserAttribute() {
        UserAttribute obj = dod.getRandomUserAttribute();
        Assert.assertNotNull("Data on demand for 'UserAttribute' failed to initialize correctly", obj);
        UserAttributePK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UserAttribute' failed to provide an identifier", id);
        obj = UserAttribute.findUserAttribute(id);
        Assert.assertNotNull("Find method for 'UserAttribute' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserAttribute' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void UserAttributeIntegrationTest.testFindAllUserAttributes() {
        Assert.assertNotNull("Data on demand for 'UserAttribute' failed to initialize correctly", dod.getRandomUserAttribute());
        long count = UserAttribute.countUserAttributes();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserAttribute', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserAttribute> result = UserAttribute.findAllUserAttributes();
        Assert.assertNotNull("Find all method for 'UserAttribute' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserAttribute' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserAttributeIntegrationTest.testFindUserAttributeEntries() {
        Assert.assertNotNull("Data on demand for 'UserAttribute' failed to initialize correctly", dod.getRandomUserAttribute());
        long count = UserAttribute.countUserAttributes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserAttribute> result = UserAttribute.findUserAttributeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserAttribute' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserAttribute' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserAttributeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserAttribute' failed to initialize correctly", dod.getRandomUserAttribute());
        UserAttribute obj = dod.getNewTransientUserAttribute(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserAttribute' failed to provide a new transient entity", obj);
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserAttribute' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void UserAttributeIntegrationTest.testRemove() {
        UserAttribute obj = dod.getRandomUserAttribute();
        Assert.assertNotNull("Data on demand for 'UserAttribute' failed to initialize correctly", obj);
        UserAttributePK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UserAttribute' failed to provide an identifier", id);
        obj = UserAttribute.findUserAttribute(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserAttribute' with identifier '" + id + "'", UserAttribute.findUserAttribute(id));
    }
    
}
