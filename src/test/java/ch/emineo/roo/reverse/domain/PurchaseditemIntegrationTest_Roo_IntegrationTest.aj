// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.domain;

import ch.emineo.roo.reverse.domain.Purchaseditem;
import ch.emineo.roo.reverse.domain.PurchaseditemDataOnDemand;
import ch.emineo.roo.reverse.domain.PurchaseditemIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PurchaseditemIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PurchaseditemIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PurchaseditemIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PurchaseditemIntegrationTest: @Transactional;
    
    @Autowired
    private PurchaseditemDataOnDemand PurchaseditemIntegrationTest.dod;
    
    @Test
    public void PurchaseditemIntegrationTest.testCountPurchaseditems() {
        Assert.assertNotNull("Data on demand for 'Purchaseditem' failed to initialize correctly", dod.getRandomPurchaseditem());
        long count = Purchaseditem.countPurchaseditems();
        Assert.assertTrue("Counter for 'Purchaseditem' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PurchaseditemIntegrationTest.testFindPurchaseditem() {
        Purchaseditem obj = dod.getRandomPurchaseditem();
        Assert.assertNotNull("Data on demand for 'Purchaseditem' failed to initialize correctly", obj);
        Integer id = obj.getPurchaseditemid();
        Assert.assertNotNull("Data on demand for 'Purchaseditem' failed to provide an identifier", id);
        obj = Purchaseditem.findPurchaseditem(id);
        Assert.assertNotNull("Find method for 'Purchaseditem' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Purchaseditem' returned the incorrect identifier", id, obj.getPurchaseditemid());
    }
    
    @Test
    public void PurchaseditemIntegrationTest.testFindAllPurchaseditems() {
        Assert.assertNotNull("Data on demand for 'Purchaseditem' failed to initialize correctly", dod.getRandomPurchaseditem());
        long count = Purchaseditem.countPurchaseditems();
        Assert.assertTrue("Too expensive to perform a find all test for 'Purchaseditem', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Purchaseditem> result = Purchaseditem.findAllPurchaseditems();
        Assert.assertNotNull("Find all method for 'Purchaseditem' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Purchaseditem' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PurchaseditemIntegrationTest.testFindPurchaseditemEntries() {
        Assert.assertNotNull("Data on demand for 'Purchaseditem' failed to initialize correctly", dod.getRandomPurchaseditem());
        long count = Purchaseditem.countPurchaseditems();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Purchaseditem> result = Purchaseditem.findPurchaseditemEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Purchaseditem' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Purchaseditem' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PurchaseditemIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Purchaseditem' failed to initialize correctly", dod.getRandomPurchaseditem());
        Purchaseditem obj = dod.getNewTransientPurchaseditem(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Purchaseditem' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Purchaseditem' identifier to be null", obj.getPurchaseditemid());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Purchaseditem' identifier to no longer be null", obj.getPurchaseditemid());
    }
    
    @Test
    public void PurchaseditemIntegrationTest.testRemove() {
        Purchaseditem obj = dod.getRandomPurchaseditem();
        Assert.assertNotNull("Data on demand for 'Purchaseditem' failed to initialize correctly", obj);
        Integer id = obj.getPurchaseditemid();
        Assert.assertNotNull("Data on demand for 'Purchaseditem' failed to provide an identifier", id);
        obj = Purchaseditem.findPurchaseditem(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Purchaseditem' with identifier '" + id + "'", Purchaseditem.findPurchaseditem(id));
    }
    
}
