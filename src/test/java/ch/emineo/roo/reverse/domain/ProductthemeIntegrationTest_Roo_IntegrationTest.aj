// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.domain;

import ch.emineo.roo.reverse.domain.Producttheme;
import ch.emineo.roo.reverse.domain.ProductthemeDataOnDemand;
import ch.emineo.roo.reverse.domain.ProductthemeIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProductthemeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProductthemeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProductthemeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProductthemeIntegrationTest: @Transactional;
    
    @Autowired
    private ProductthemeDataOnDemand ProductthemeIntegrationTest.dod;
    
    @Test
    public void ProductthemeIntegrationTest.testCountProductthemes() {
        Assert.assertNotNull("Data on demand for 'Producttheme' failed to initialize correctly", dod.getRandomProducttheme());
        long count = Producttheme.countProductthemes();
        Assert.assertTrue("Counter for 'Producttheme' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProductthemeIntegrationTest.testFindProducttheme() {
        Producttheme obj = dod.getRandomProducttheme();
        Assert.assertNotNull("Data on demand for 'Producttheme' failed to initialize correctly", obj);
        Integer id = obj.getProductthemeid();
        Assert.assertNotNull("Data on demand for 'Producttheme' failed to provide an identifier", id);
        obj = Producttheme.findProducttheme(id);
        Assert.assertNotNull("Find method for 'Producttheme' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Producttheme' returned the incorrect identifier", id, obj.getProductthemeid());
    }
    
    @Test
    public void ProductthemeIntegrationTest.testFindAllProductthemes() {
        Assert.assertNotNull("Data on demand for 'Producttheme' failed to initialize correctly", dod.getRandomProducttheme());
        long count = Producttheme.countProductthemes();
        Assert.assertTrue("Too expensive to perform a find all test for 'Producttheme', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Producttheme> result = Producttheme.findAllProductthemes();
        Assert.assertNotNull("Find all method for 'Producttheme' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Producttheme' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProductthemeIntegrationTest.testFindProductthemeEntries() {
        Assert.assertNotNull("Data on demand for 'Producttheme' failed to initialize correctly", dod.getRandomProducttheme());
        long count = Producttheme.countProductthemes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Producttheme> result = Producttheme.findProductthemeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Producttheme' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Producttheme' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProductthemeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Producttheme' failed to initialize correctly", dod.getRandomProducttheme());
        Producttheme obj = dod.getNewTransientProducttheme(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Producttheme' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Producttheme' identifier to be null", obj.getProductthemeid());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Producttheme' identifier to no longer be null", obj.getProductthemeid());
    }
    
    @Test
    public void ProductthemeIntegrationTest.testRemove() {
        Producttheme obj = dod.getRandomProducttheme();
        Assert.assertNotNull("Data on demand for 'Producttheme' failed to initialize correctly", obj);
        Integer id = obj.getProductthemeid();
        Assert.assertNotNull("Data on demand for 'Producttheme' failed to provide an identifier", id);
        obj = Producttheme.findProducttheme(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Producttheme' with identifier '" + id + "'", Producttheme.findProducttheme(id));
    }
    
}
