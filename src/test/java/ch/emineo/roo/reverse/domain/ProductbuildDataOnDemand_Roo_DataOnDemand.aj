// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.domain;

import ch.emineo.roo.reverse.domain.Productbuild;
import ch.emineo.roo.reverse.domain.ProductbuildDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ProductbuildDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ProductbuildDataOnDemand: @Component;
    
    private Random ProductbuildDataOnDemand.rnd = new SecureRandom();
    
    private List<Productbuild> ProductbuildDataOnDemand.data;
    
    public Productbuild ProductbuildDataOnDemand.getNewTransientProductbuild(int index) {
        Productbuild obj = new Productbuild();
        setOnline(obj, index);
        setPicturepath(obj, index);
        setPublic1(obj, index);
        setSample(obj, index);
        return obj;
    }
    
    public void ProductbuildDataOnDemand.setOnline(Productbuild obj, int index) {
        Boolean online = true;
        obj.setOnline(online);
    }
    
    public void ProductbuildDataOnDemand.setPicturepath(Productbuild obj, int index) {
        String picturepath = "picturepath_" + index;
        if (picturepath.length() > 255) {
            picturepath = picturepath.substring(0, 255);
        }
        obj.setPicturepath(picturepath);
    }
    
    public void ProductbuildDataOnDemand.setPublic1(Productbuild obj, int index) {
        Boolean public1 = true;
        obj.setPublic1(public1);
    }
    
    public void ProductbuildDataOnDemand.setSample(Productbuild obj, int index) {
        Boolean sample = true;
        obj.setSample(sample);
    }
    
    public Productbuild ProductbuildDataOnDemand.getSpecificProductbuild(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Productbuild obj = data.get(index);
        Integer id = obj.getProductbuildid();
        return Productbuild.findProductbuild(id);
    }
    
    public Productbuild ProductbuildDataOnDemand.getRandomProductbuild() {
        init();
        Productbuild obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getProductbuildid();
        return Productbuild.findProductbuild(id);
    }
    
    public boolean ProductbuildDataOnDemand.modifyProductbuild(Productbuild obj) {
        return false;
    }
    
    public void ProductbuildDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Productbuild.findProductbuildEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Productbuild' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Productbuild>();
        for (int i = 0; i < 10; i++) {
            Productbuild obj = getNewTransientProductbuild(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
