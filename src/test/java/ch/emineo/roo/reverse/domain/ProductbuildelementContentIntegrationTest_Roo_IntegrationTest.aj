// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.domain;

import ch.emineo.roo.reverse.domain.ProductbuildelementContent;
import ch.emineo.roo.reverse.domain.ProductbuildelementContentDataOnDemand;
import ch.emineo.roo.reverse.domain.ProductbuildelementContentIntegrationTest;
import ch.emineo.roo.reverse.domain.ProductbuildelementContentPK;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProductbuildelementContentIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProductbuildelementContentIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProductbuildelementContentIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProductbuildelementContentIntegrationTest: @Transactional;
    
    @Autowired
    private ProductbuildelementContentDataOnDemand ProductbuildelementContentIntegrationTest.dod;
    
    @Test
    public void ProductbuildelementContentIntegrationTest.testCountProductbuildelementContents() {
        Assert.assertNotNull("Data on demand for 'ProductbuildelementContent' failed to initialize correctly", dod.getRandomProductbuildelementContent());
        long count = ProductbuildelementContent.countProductbuildelementContents();
        Assert.assertTrue("Counter for 'ProductbuildelementContent' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProductbuildelementContentIntegrationTest.testFindProductbuildelementContent() {
        ProductbuildelementContent obj = dod.getRandomProductbuildelementContent();
        Assert.assertNotNull("Data on demand for 'ProductbuildelementContent' failed to initialize correctly", obj);
        ProductbuildelementContentPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductbuildelementContent' failed to provide an identifier", id);
        obj = ProductbuildelementContent.findProductbuildelementContent(id);
        Assert.assertNotNull("Find method for 'ProductbuildelementContent' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ProductbuildelementContent' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProductbuildelementContentIntegrationTest.testFindAllProductbuildelementContents() {
        Assert.assertNotNull("Data on demand for 'ProductbuildelementContent' failed to initialize correctly", dod.getRandomProductbuildelementContent());
        long count = ProductbuildelementContent.countProductbuildelementContents();
        Assert.assertTrue("Too expensive to perform a find all test for 'ProductbuildelementContent', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ProductbuildelementContent> result = ProductbuildelementContent.findAllProductbuildelementContents();
        Assert.assertNotNull("Find all method for 'ProductbuildelementContent' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ProductbuildelementContent' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProductbuildelementContentIntegrationTest.testFindProductbuildelementContentEntries() {
        Assert.assertNotNull("Data on demand for 'ProductbuildelementContent' failed to initialize correctly", dod.getRandomProductbuildelementContent());
        long count = ProductbuildelementContent.countProductbuildelementContents();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ProductbuildelementContent> result = ProductbuildelementContent.findProductbuildelementContentEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ProductbuildelementContent' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ProductbuildelementContent' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProductbuildelementContentIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ProductbuildelementContent' failed to initialize correctly", dod.getRandomProductbuildelementContent());
        ProductbuildelementContent obj = dod.getNewTransientProductbuildelementContent(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ProductbuildelementContent' failed to provide a new transient entity", obj);
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ProductbuildelementContent' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ProductbuildelementContentIntegrationTest.testRemove() {
        ProductbuildelementContent obj = dod.getRandomProductbuildelementContent();
        Assert.assertNotNull("Data on demand for 'ProductbuildelementContent' failed to initialize correctly", obj);
        ProductbuildelementContentPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductbuildelementContent' failed to provide an identifier", id);
        obj = ProductbuildelementContent.findProductbuildelementContent(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ProductbuildelementContent' with identifier '" + id + "'", ProductbuildelementContent.findProductbuildelementContent(id));
    }
    
}
