// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.domain;

import ch.emineo.roo.reverse.domain.Role;
import ch.emineo.roo.reverse.domain.RoleDataOnDemand;
import ch.emineo.roo.reverse.domain.User;
import ch.emineo.roo.reverse.domain.UserDataOnDemand;
import ch.emineo.roo.reverse.domain.UserRole;
import ch.emineo.roo.reverse.domain.UserRoleDataOnDemand;
import ch.emineo.roo.reverse.domain.UserRolePK;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect UserRoleDataOnDemand_Roo_DataOnDemand {
    
    declare @type: UserRoleDataOnDemand: @Component;
    
    private Random UserRoleDataOnDemand.rnd = new SecureRandom();
    
    private List<UserRole> UserRoleDataOnDemand.data;
    
    @Autowired
    private RoleDataOnDemand UserRoleDataOnDemand.roleDataOnDemand;
    
    @Autowired
    private UserDataOnDemand UserRoleDataOnDemand.userDataOnDemand;
    
    public UserRole UserRoleDataOnDemand.getNewTransientUserRole(int index) {
        UserRole obj = new UserRole();
        setEmbeddedIdClass(obj, index);
        setRolesRoleid(obj, index);
        setUserUserid(obj, index);
        return obj;
    }
    
    public void UserRoleDataOnDemand.setEmbeddedIdClass(UserRole obj, int index) {
        Integer userUserid = new Integer(index);
        Integer idx = new Integer(index);
        
        UserRolePK embeddedIdClass = new UserRolePK(userUserid, idx);
        obj.setId(embeddedIdClass);
    }
    
    public void UserRoleDataOnDemand.setRolesRoleid(UserRole obj, int index) {
        Role rolesRoleid = roleDataOnDemand.getRandomRole();
        obj.setRolesRoleid(rolesRoleid);
    }
    
    public void UserRoleDataOnDemand.setUserUserid(UserRole obj, int index) {
        User userUserid = userDataOnDemand.getRandomUser();
        obj.setUserUserid(userUserid);
    }
    
    public UserRole UserRoleDataOnDemand.getSpecificUserRole(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        UserRole obj = data.get(index);
        UserRolePK id = obj.getId();
        return UserRole.findUserRole(id);
    }
    
    public UserRole UserRoleDataOnDemand.getRandomUserRole() {
        init();
        UserRole obj = data.get(rnd.nextInt(data.size()));
        UserRolePK id = obj.getId();
        return UserRole.findUserRole(id);
    }
    
    public boolean UserRoleDataOnDemand.modifyUserRole(UserRole obj) {
        return false;
    }
    
    public void UserRoleDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = UserRole.findUserRoleEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'UserRole' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<UserRole>();
        for (int i = 0; i < 10; i++) {
            UserRole obj = getNewTransientUserRole(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
