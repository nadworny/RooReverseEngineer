// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.domain;

import ch.emineo.roo.reverse.domain.ProductAttribute;
import ch.emineo.roo.reverse.domain.ProductAttributeDataOnDemand;
import ch.emineo.roo.reverse.domain.ProductAttributeIntegrationTest;
import ch.emineo.roo.reverse.domain.ProductAttributePK;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProductAttributeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProductAttributeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProductAttributeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProductAttributeIntegrationTest: @Transactional;
    
    @Autowired
    private ProductAttributeDataOnDemand ProductAttributeIntegrationTest.dod;
    
    @Test
    public void ProductAttributeIntegrationTest.testCountProductAttributes() {
        Assert.assertNotNull("Data on demand for 'ProductAttribute' failed to initialize correctly", dod.getRandomProductAttribute());
        long count = ProductAttribute.countProductAttributes();
        Assert.assertTrue("Counter for 'ProductAttribute' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProductAttributeIntegrationTest.testFindProductAttribute() {
        ProductAttribute obj = dod.getRandomProductAttribute();
        Assert.assertNotNull("Data on demand for 'ProductAttribute' failed to initialize correctly", obj);
        ProductAttributePK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductAttribute' failed to provide an identifier", id);
        obj = ProductAttribute.findProductAttribute(id);
        Assert.assertNotNull("Find method for 'ProductAttribute' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ProductAttribute' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProductAttributeIntegrationTest.testFindAllProductAttributes() {
        Assert.assertNotNull("Data on demand for 'ProductAttribute' failed to initialize correctly", dod.getRandomProductAttribute());
        long count = ProductAttribute.countProductAttributes();
        Assert.assertTrue("Too expensive to perform a find all test for 'ProductAttribute', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ProductAttribute> result = ProductAttribute.findAllProductAttributes();
        Assert.assertNotNull("Find all method for 'ProductAttribute' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ProductAttribute' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProductAttributeIntegrationTest.testFindProductAttributeEntries() {
        Assert.assertNotNull("Data on demand for 'ProductAttribute' failed to initialize correctly", dod.getRandomProductAttribute());
        long count = ProductAttribute.countProductAttributes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ProductAttribute> result = ProductAttribute.findProductAttributeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ProductAttribute' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ProductAttribute' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProductAttributeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ProductAttribute' failed to initialize correctly", dod.getRandomProductAttribute());
        ProductAttribute obj = dod.getNewTransientProductAttribute(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ProductAttribute' failed to provide a new transient entity", obj);
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ProductAttribute' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ProductAttributeIntegrationTest.testRemove() {
        ProductAttribute obj = dod.getRandomProductAttribute();
        Assert.assertNotNull("Data on demand for 'ProductAttribute' failed to initialize correctly", obj);
        ProductAttributePK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductAttribute' failed to provide an identifier", id);
        obj = ProductAttribute.findProductAttribute(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ProductAttribute' with identifier '" + id + "'", ProductAttribute.findProductAttribute(id));
    }
    
}
