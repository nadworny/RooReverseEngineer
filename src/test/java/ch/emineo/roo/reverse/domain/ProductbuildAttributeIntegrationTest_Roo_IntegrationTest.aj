// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.domain;

import ch.emineo.roo.reverse.domain.ProductbuildAttribute;
import ch.emineo.roo.reverse.domain.ProductbuildAttributeDataOnDemand;
import ch.emineo.roo.reverse.domain.ProductbuildAttributeIntegrationTest;
import ch.emineo.roo.reverse.domain.ProductbuildAttributePK;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProductbuildAttributeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProductbuildAttributeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProductbuildAttributeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProductbuildAttributeIntegrationTest: @Transactional;
    
    @Autowired
    private ProductbuildAttributeDataOnDemand ProductbuildAttributeIntegrationTest.dod;
    
    @Test
    public void ProductbuildAttributeIntegrationTest.testCountProductbuildAttributes() {
        Assert.assertNotNull("Data on demand for 'ProductbuildAttribute' failed to initialize correctly", dod.getRandomProductbuildAttribute());
        long count = ProductbuildAttribute.countProductbuildAttributes();
        Assert.assertTrue("Counter for 'ProductbuildAttribute' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProductbuildAttributeIntegrationTest.testFindProductbuildAttribute() {
        ProductbuildAttribute obj = dod.getRandomProductbuildAttribute();
        Assert.assertNotNull("Data on demand for 'ProductbuildAttribute' failed to initialize correctly", obj);
        ProductbuildAttributePK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductbuildAttribute' failed to provide an identifier", id);
        obj = ProductbuildAttribute.findProductbuildAttribute(id);
        Assert.assertNotNull("Find method for 'ProductbuildAttribute' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ProductbuildAttribute' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProductbuildAttributeIntegrationTest.testFindAllProductbuildAttributes() {
        Assert.assertNotNull("Data on demand for 'ProductbuildAttribute' failed to initialize correctly", dod.getRandomProductbuildAttribute());
        long count = ProductbuildAttribute.countProductbuildAttributes();
        Assert.assertTrue("Too expensive to perform a find all test for 'ProductbuildAttribute', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ProductbuildAttribute> result = ProductbuildAttribute.findAllProductbuildAttributes();
        Assert.assertNotNull("Find all method for 'ProductbuildAttribute' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ProductbuildAttribute' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProductbuildAttributeIntegrationTest.testFindProductbuildAttributeEntries() {
        Assert.assertNotNull("Data on demand for 'ProductbuildAttribute' failed to initialize correctly", dod.getRandomProductbuildAttribute());
        long count = ProductbuildAttribute.countProductbuildAttributes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ProductbuildAttribute> result = ProductbuildAttribute.findProductbuildAttributeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ProductbuildAttribute' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ProductbuildAttribute' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProductbuildAttributeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ProductbuildAttribute' failed to initialize correctly", dod.getRandomProductbuildAttribute());
        ProductbuildAttribute obj = dod.getNewTransientProductbuildAttribute(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ProductbuildAttribute' failed to provide a new transient entity", obj);
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ProductbuildAttribute' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ProductbuildAttributeIntegrationTest.testRemove() {
        ProductbuildAttribute obj = dod.getRandomProductbuildAttribute();
        Assert.assertNotNull("Data on demand for 'ProductbuildAttribute' failed to initialize correctly", obj);
        ProductbuildAttributePK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductbuildAttribute' failed to provide an identifier", id);
        obj = ProductbuildAttribute.findProductbuildAttribute(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ProductbuildAttribute' with identifier '" + id + "'", ProductbuildAttribute.findProductbuildAttribute(id));
    }
    
}
