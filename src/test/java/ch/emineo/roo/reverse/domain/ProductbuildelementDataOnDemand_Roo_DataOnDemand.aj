// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.domain;

import ch.emineo.roo.reverse.domain.Productbuildelement;
import ch.emineo.roo.reverse.domain.ProductbuildelementDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ProductbuildelementDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ProductbuildelementDataOnDemand: @Component;
    
    private Random ProductbuildelementDataOnDemand.rnd = new SecureRandom();
    
    private List<Productbuildelement> ProductbuildelementDataOnDemand.data;
    
    public Productbuildelement ProductbuildelementDataOnDemand.getNewTransientProductbuildelement(int index) {
        Productbuildelement obj = new Productbuildelement();
        setContenttype(obj, index);
        setLanguage(obj, index);
        setOnline(obj, index);
        setPublic1(obj, index);
        setSample(obj, index);
        return obj;
    }
    
    public void ProductbuildelementDataOnDemand.setContenttype(Productbuildelement obj, int index) {
        String contenttype = "contenttype_" + index;
        if (contenttype.length() > 255) {
            contenttype = contenttype.substring(0, 255);
        }
        obj.setContenttype(contenttype);
    }
    
    public void ProductbuildelementDataOnDemand.setLanguage(Productbuildelement obj, int index) {
        String language = "language_" + index;
        if (language.length() > 255) {
            language = language.substring(0, 255);
        }
        obj.setLanguage(language);
    }
    
    public void ProductbuildelementDataOnDemand.setOnline(Productbuildelement obj, int index) {
        Boolean online = true;
        obj.setOnline(online);
    }
    
    public void ProductbuildelementDataOnDemand.setPublic1(Productbuildelement obj, int index) {
        Boolean public1 = true;
        obj.setPublic1(public1);
    }
    
    public void ProductbuildelementDataOnDemand.setSample(Productbuildelement obj, int index) {
        Boolean sample = true;
        obj.setSample(sample);
    }
    
    public Productbuildelement ProductbuildelementDataOnDemand.getSpecificProductbuildelement(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Productbuildelement obj = data.get(index);
        Integer id = obj.getProductbuildelementid();
        return Productbuildelement.findProductbuildelement(id);
    }
    
    public Productbuildelement ProductbuildelementDataOnDemand.getRandomProductbuildelement() {
        init();
        Productbuildelement obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getProductbuildelementid();
        return Productbuildelement.findProductbuildelement(id);
    }
    
    public boolean ProductbuildelementDataOnDemand.modifyProductbuildelement(Productbuildelement obj) {
        return false;
    }
    
    public void ProductbuildelementDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Productbuildelement.findProductbuildelementEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Productbuildelement' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Productbuildelement>();
        for (int i = 0; i < 10; i++) {
            Productbuildelement obj = getNewTransientProductbuildelement(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
