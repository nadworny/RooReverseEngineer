// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.domain;

import ch.emineo.roo.reverse.domain.ProductProductvariant;
import ch.emineo.roo.reverse.domain.ProductProductvariantDataOnDemand;
import ch.emineo.roo.reverse.domain.ProductProductvariantIntegrationTest;
import ch.emineo.roo.reverse.domain.ProductProductvariantPK;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProductProductvariantIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProductProductvariantIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProductProductvariantIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProductProductvariantIntegrationTest: @Transactional;
    
    @Autowired
    private ProductProductvariantDataOnDemand ProductProductvariantIntegrationTest.dod;
    
    @Test
    public void ProductProductvariantIntegrationTest.testCountProductProductvariants() {
        Assert.assertNotNull("Data on demand for 'ProductProductvariant' failed to initialize correctly", dod.getRandomProductProductvariant());
        long count = ProductProductvariant.countProductProductvariants();
        Assert.assertTrue("Counter for 'ProductProductvariant' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProductProductvariantIntegrationTest.testFindProductProductvariant() {
        ProductProductvariant obj = dod.getRandomProductProductvariant();
        Assert.assertNotNull("Data on demand for 'ProductProductvariant' failed to initialize correctly", obj);
        ProductProductvariantPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductProductvariant' failed to provide an identifier", id);
        obj = ProductProductvariant.findProductProductvariant(id);
        Assert.assertNotNull("Find method for 'ProductProductvariant' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ProductProductvariant' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProductProductvariantIntegrationTest.testFindAllProductProductvariants() {
        Assert.assertNotNull("Data on demand for 'ProductProductvariant' failed to initialize correctly", dod.getRandomProductProductvariant());
        long count = ProductProductvariant.countProductProductvariants();
        Assert.assertTrue("Too expensive to perform a find all test for 'ProductProductvariant', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ProductProductvariant> result = ProductProductvariant.findAllProductProductvariants();
        Assert.assertNotNull("Find all method for 'ProductProductvariant' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ProductProductvariant' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProductProductvariantIntegrationTest.testFindProductProductvariantEntries() {
        Assert.assertNotNull("Data on demand for 'ProductProductvariant' failed to initialize correctly", dod.getRandomProductProductvariant());
        long count = ProductProductvariant.countProductProductvariants();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ProductProductvariant> result = ProductProductvariant.findProductProductvariantEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ProductProductvariant' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ProductProductvariant' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProductProductvariantIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ProductProductvariant' failed to initialize correctly", dod.getRandomProductProductvariant());
        ProductProductvariant obj = dod.getNewTransientProductProductvariant(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ProductProductvariant' failed to provide a new transient entity", obj);
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ProductProductvariant' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ProductProductvariantIntegrationTest.testRemove() {
        ProductProductvariant obj = dod.getRandomProductProductvariant();
        Assert.assertNotNull("Data on demand for 'ProductProductvariant' failed to initialize correctly", obj);
        ProductProductvariantPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductProductvariant' failed to provide an identifier", id);
        obj = ProductProductvariant.findProductProductvariant(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ProductProductvariant' with identifier '" + id + "'", ProductProductvariant.findProductProductvariant(id));
    }
    
}
