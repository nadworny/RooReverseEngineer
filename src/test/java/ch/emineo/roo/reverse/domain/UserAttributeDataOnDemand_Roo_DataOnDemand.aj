// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.domain;

import ch.emineo.roo.reverse.domain.Attribute;
import ch.emineo.roo.reverse.domain.AttributeDataOnDemand;
import ch.emineo.roo.reverse.domain.User;
import ch.emineo.roo.reverse.domain.UserAttribute;
import ch.emineo.roo.reverse.domain.UserAttributeDataOnDemand;
import ch.emineo.roo.reverse.domain.UserAttributePK;
import ch.emineo.roo.reverse.domain.UserDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect UserAttributeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: UserAttributeDataOnDemand: @Component;
    
    private Random UserAttributeDataOnDemand.rnd = new SecureRandom();
    
    private List<UserAttribute> UserAttributeDataOnDemand.data;
    
    @Autowired
    private AttributeDataOnDemand UserAttributeDataOnDemand.attributeDataOnDemand;
    
    @Autowired
    private UserDataOnDemand UserAttributeDataOnDemand.userDataOnDemand;
    
    public UserAttribute UserAttributeDataOnDemand.getNewTransientUserAttribute(int index) {
        UserAttribute obj = new UserAttribute();
        setEmbeddedIdClass(obj, index);
        setAttributesAttributeid(obj, index);
        setUserUserid(obj, index);
        return obj;
    }
    
    public void UserAttributeDataOnDemand.setEmbeddedIdClass(UserAttribute obj, int index) {
        Integer userUserid = new Integer(index);
        Integer idx = new Integer(index);
        
        UserAttributePK embeddedIdClass = new UserAttributePK(userUserid, idx);
        obj.setId(embeddedIdClass);
    }
    
    public void UserAttributeDataOnDemand.setAttributesAttributeid(UserAttribute obj, int index) {
        Attribute attributesAttributeid = attributeDataOnDemand.getRandomAttribute();
        obj.setAttributesAttributeid(attributesAttributeid);
    }
    
    public void UserAttributeDataOnDemand.setUserUserid(UserAttribute obj, int index) {
        User userUserid = userDataOnDemand.getRandomUser();
        obj.setUserUserid(userUserid);
    }
    
    public UserAttribute UserAttributeDataOnDemand.getSpecificUserAttribute(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        UserAttribute obj = data.get(index);
        UserAttributePK id = obj.getId();
        return UserAttribute.findUserAttribute(id);
    }
    
    public UserAttribute UserAttributeDataOnDemand.getRandomUserAttribute() {
        init();
        UserAttribute obj = data.get(rnd.nextInt(data.size()));
        UserAttributePK id = obj.getId();
        return UserAttribute.findUserAttribute(id);
    }
    
    public boolean UserAttributeDataOnDemand.modifyUserAttribute(UserAttribute obj) {
        return false;
    }
    
    public void UserAttributeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = UserAttribute.findUserAttributeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'UserAttribute' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<UserAttribute>();
        for (int i = 0; i < 10; i++) {
            UserAttribute obj = getNewTransientUserAttribute(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
