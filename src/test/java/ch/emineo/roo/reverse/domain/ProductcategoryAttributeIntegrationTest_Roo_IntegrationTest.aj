// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.domain;

import ch.emineo.roo.reverse.domain.ProductcategoryAttribute;
import ch.emineo.roo.reverse.domain.ProductcategoryAttributeDataOnDemand;
import ch.emineo.roo.reverse.domain.ProductcategoryAttributeIntegrationTest;
import ch.emineo.roo.reverse.domain.ProductcategoryAttributePK;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProductcategoryAttributeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProductcategoryAttributeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProductcategoryAttributeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProductcategoryAttributeIntegrationTest: @Transactional;
    
    @Autowired
    private ProductcategoryAttributeDataOnDemand ProductcategoryAttributeIntegrationTest.dod;
    
    @Test
    public void ProductcategoryAttributeIntegrationTest.testCountProductcategoryAttributes() {
        Assert.assertNotNull("Data on demand for 'ProductcategoryAttribute' failed to initialize correctly", dod.getRandomProductcategoryAttribute());
        long count = ProductcategoryAttribute.countProductcategoryAttributes();
        Assert.assertTrue("Counter for 'ProductcategoryAttribute' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProductcategoryAttributeIntegrationTest.testFindProductcategoryAttribute() {
        ProductcategoryAttribute obj = dod.getRandomProductcategoryAttribute();
        Assert.assertNotNull("Data on demand for 'ProductcategoryAttribute' failed to initialize correctly", obj);
        ProductcategoryAttributePK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductcategoryAttribute' failed to provide an identifier", id);
        obj = ProductcategoryAttribute.findProductcategoryAttribute(id);
        Assert.assertNotNull("Find method for 'ProductcategoryAttribute' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ProductcategoryAttribute' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProductcategoryAttributeIntegrationTest.testFindAllProductcategoryAttributes() {
        Assert.assertNotNull("Data on demand for 'ProductcategoryAttribute' failed to initialize correctly", dod.getRandomProductcategoryAttribute());
        long count = ProductcategoryAttribute.countProductcategoryAttributes();
        Assert.assertTrue("Too expensive to perform a find all test for 'ProductcategoryAttribute', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ProductcategoryAttribute> result = ProductcategoryAttribute.findAllProductcategoryAttributes();
        Assert.assertNotNull("Find all method for 'ProductcategoryAttribute' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ProductcategoryAttribute' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProductcategoryAttributeIntegrationTest.testFindProductcategoryAttributeEntries() {
        Assert.assertNotNull("Data on demand for 'ProductcategoryAttribute' failed to initialize correctly", dod.getRandomProductcategoryAttribute());
        long count = ProductcategoryAttribute.countProductcategoryAttributes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ProductcategoryAttribute> result = ProductcategoryAttribute.findProductcategoryAttributeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ProductcategoryAttribute' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ProductcategoryAttribute' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProductcategoryAttributeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ProductcategoryAttribute' failed to initialize correctly", dod.getRandomProductcategoryAttribute());
        ProductcategoryAttribute obj = dod.getNewTransientProductcategoryAttribute(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ProductcategoryAttribute' failed to provide a new transient entity", obj);
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ProductcategoryAttribute' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ProductcategoryAttributeIntegrationTest.testRemove() {
        ProductcategoryAttribute obj = dod.getRandomProductcategoryAttribute();
        Assert.assertNotNull("Data on demand for 'ProductcategoryAttribute' failed to initialize correctly", obj);
        ProductcategoryAttributePK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductcategoryAttribute' failed to provide an identifier", id);
        obj = ProductcategoryAttribute.findProductcategoryAttribute(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ProductcategoryAttribute' with identifier '" + id + "'", ProductcategoryAttribute.findProductcategoryAttribute(id));
    }
    
}
