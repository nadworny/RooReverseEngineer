// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.emineo.roo.reverse.web;

import ch.emineo.roo.reverse.domain.Productcategory;
import ch.emineo.roo.reverse.domain.ProductcategoryProductsubcategory;
import ch.emineo.roo.reverse.domain.ProductcategoryProductsubcategoryPK;
import ch.emineo.roo.reverse.domain.Productsubcategory;
import ch.emineo.roo.reverse.web.ProductcategoryProductsubcategoryController;
import java.io.UnsupportedEncodingException;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.core.convert.ConversionService;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;

privileged aspect ProductcategoryProductsubcategoryController_Roo_Controller {
    
    private ConversionService ProductcategoryProductsubcategoryController.conversionService;
    
    @Autowired
    public ProductcategoryProductsubcategoryController.new(ConversionService conversionService) {
        this.conversionService = conversionService;
    }

    @RequestMapping(method = RequestMethod.POST, produces = "text/html")
    public String ProductcategoryProductsubcategoryController.create(@Valid ProductcategoryProductsubcategory productcategoryProductsubcategory, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, productcategoryProductsubcategory);
            return "productcategoryproductsubcategorys/create";
        }
        uiModel.asMap().clear();
        productcategoryProductsubcategory.persist();
        return "redirect:/productcategoryproductsubcategorys/" + encodeUrlPathSegment(conversionService.convert(productcategoryProductsubcategory.getId(), String.class), httpServletRequest);
    }
    
    @RequestMapping(params = "form", produces = "text/html")
    public String ProductcategoryProductsubcategoryController.createForm(Model uiModel) {
        populateEditForm(uiModel, new ProductcategoryProductsubcategory());
        return "productcategoryproductsubcategorys/create";
    }
    
    @RequestMapping(value = "/{id}", produces = "text/html")
    public String ProductcategoryProductsubcategoryController.show(@PathVariable("id") ProductcategoryProductsubcategoryPK id, Model uiModel) {
        uiModel.addAttribute("productcategoryproductsubcategory", ProductcategoryProductsubcategory.findProductcategoryProductsubcategory(id));
        uiModel.addAttribute("itemId", conversionService.convert(id, String.class));
        return "productcategoryproductsubcategorys/show";
    }
    
    @RequestMapping(produces = "text/html")
    public String ProductcategoryProductsubcategoryController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            final int firstResult = page == null ? 0 : (page.intValue() - 1) * sizeNo;
            uiModel.addAttribute("productcategoryproductsubcategorys", ProductcategoryProductsubcategory.findProductcategoryProductsubcategoryEntries(firstResult, sizeNo));
            float nrOfPages = (float) ProductcategoryProductsubcategory.countProductcategoryProductsubcategorys() / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("productcategoryproductsubcategorys", ProductcategoryProductsubcategory.findAllProductcategoryProductsubcategorys());
        }
        return "productcategoryproductsubcategorys/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT, produces = "text/html")
    public String ProductcategoryProductsubcategoryController.update(@Valid ProductcategoryProductsubcategory productcategoryProductsubcategory, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, productcategoryProductsubcategory);
            return "productcategoryproductsubcategorys/update";
        }
        uiModel.asMap().clear();
        productcategoryProductsubcategory.merge();
        return "redirect:/productcategoryproductsubcategorys/" + encodeUrlPathSegment(conversionService.convert(productcategoryProductsubcategory.getId(), String.class), httpServletRequest);
    }
    
    @RequestMapping(value = "/{id}", params = "form", produces = "text/html")
    public String ProductcategoryProductsubcategoryController.updateForm(@PathVariable("id") ProductcategoryProductsubcategoryPK id, Model uiModel) {
        populateEditForm(uiModel, ProductcategoryProductsubcategory.findProductcategoryProductsubcategory(id));
        return "productcategoryproductsubcategorys/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE, produces = "text/html")
    public String ProductcategoryProductsubcategoryController.delete(@PathVariable("id") ProductcategoryProductsubcategoryPK id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        ProductcategoryProductsubcategory productcategoryProductsubcategory = ProductcategoryProductsubcategory.findProductcategoryProductsubcategory(id);
        productcategoryProductsubcategory.remove();
        uiModel.asMap().clear();
        uiModel.addAttribute("page", (page == null) ? "1" : page.toString());
        uiModel.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/productcategoryproductsubcategorys";
    }
    
    void ProductcategoryProductsubcategoryController.populateEditForm(Model uiModel, ProductcategoryProductsubcategory productcategoryProductsubcategory) {
        uiModel.addAttribute("productcategoryProductsubcategory", productcategoryProductsubcategory);
        uiModel.addAttribute("productcategorys", Productcategory.findAllProductcategorys());
        uiModel.addAttribute("productsubcategorys", Productsubcategory.findAllProductsubcategorys());
    }
    
    String ProductcategoryProductsubcategoryController.encodeUrlPathSegment(String pathSegment, HttpServletRequest httpServletRequest) {
        String enc = httpServletRequest.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        } catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
